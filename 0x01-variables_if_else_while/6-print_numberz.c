#include <stdio.h>
#include <stdlib.h>

/**
 * main - "print 10 digits"
 * Return: Always return 0.
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)

		putchar(a);
	putchar('\n');
	return (0);
}
